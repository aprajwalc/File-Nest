// Generated by CoffeeScript 1.12.7
(function() {
  var _, render, ruleTemplate, rules;

  _ = require("underscore");

  rules = (require("./coffeelint")).RULES;

  render = function() {
    var i, len, ref, results, rule, ruleName, rulesHTML;
    rulesHTML = "";
    ref = _.sortBy(_.keys(rules), (function(s) {
      return s;
    }));
    results = [];
    for (i = 0, len = ref.length; i < len; i++) {
      ruleName = ref[i];
      rule = rules[ruleName];
      rule.name = ruleName;
      if (!rule.description) {
        rule.description = "[no description provided]";
      }
      results.push(console.log(ruleTemplate(rule)));
    }
    return results;
  };

  ruleTemplate = _.template("<tr>\n<td class=\"rule\"><%= name %></td>\n<td class=\"description\">\n    <%= description %>\n    <p><em>default level: <%= level %></em></p>\n</td>\n</tr>");

  render();

}).call(this);
